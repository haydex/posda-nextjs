export default function DashboardSettings() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight">
        Dashboard Settings
      </h1>
      <p className="page-subtitle">
        This page is rendered at <code>/dashboard/settings</code> and uses the
        same nested layout.
      </p>
    </section>
  );
}
